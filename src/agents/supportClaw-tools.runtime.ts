/**
 * Runtime seam for tests and lazy imports that need SupportClaw tool creation
 * without depending on the full tool module path.
 */
export { createSupportClawTools } from "./supportClaw-tools.js";
