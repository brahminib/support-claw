# SupportClaw Cohere Provider

Official SupportClaw provider plugin for Cohere's OpenAI-compatible Compatibility
API.

Install from SupportClaw:

```bash
supportClaw plugins install @supportclaw/cohere-provider
supportClaw gateway restart
```

Configure a Cohere API key, then select `cohere/command-a-03-2025`.
