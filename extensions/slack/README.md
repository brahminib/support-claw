# SupportClaw Slack

Official SupportClaw channel plugin for Slack channels, DMs, commands, and app events.

Install from SupportClaw:

```bash
supportClaw plugin add @supportclaw/slack
```

Configure the Slack app credentials and allowed workspaces/channels in SupportClaw. The plugin lets agents receive Slack events and reply through the configured Slack app.
