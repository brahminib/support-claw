# SupportClaw Discord

Official SupportClaw channel plugin for Discord servers, channels, DMs, slash commands, and app events.

Install from SupportClaw:

```bash
supportClaw plugin add @supportclaw/discord
```

Configure a Discord bot token and the channels or servers SupportClaw should handle. The plugin lets SupportClaw agents receive Discord messages and respond through the configured Discord app.
