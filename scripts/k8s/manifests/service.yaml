apiVersion: v1
kind: Service
metadata:
  name: supportClaw
  labels:
    app: supportClaw
spec:
  type: ClusterIP
  selector:
    app: supportClaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
